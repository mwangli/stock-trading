# 模块一：数据采集 - 测试用例文档

## 文档信息
- **模块**: MOD-001 数据采集
- **版本**: 1.0
- **创建日期**: 2026-02-19
- **测试类型**: 单元测试 + 集成测试

---

## 1. 测试范围

### 1.1 测试目标
验证数据采集模块的所有功能是否按需求正常工作，包括：
- 股票列表获取
- 历史K线数据获取
- 实时行情获取
- 数据同步功能
- 定时任务执行

### 1.2 测试环境
- **JDK**: 17
- **数据库**: MySQL 8.0 + MongoDB 5.0
- **缓存**: Redis 6.0
- **测试框架**: JUnit 5 + Mockito

---

## 2. 单元测试用例

### 2.1 AKToolsClientTest - API客户端测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-001-001 | 测试获取股票列表 | 网络连接正常 | 1. 调用getStockList()<br>2. 验证返回结果 | 1. 返回非空列表<br>2. 数量>4000条<br>3. 每条包含code,name,market | P0 |
| TC-001-002 | 测试获取股票列表-网络异常 | 模拟网络超时 | 1. 模拟HTTP超时<br>2. 调用getStockList() | 1. 重试3次<br>2. 返回空列表<br>3. 记录错误日志 | P0 |
| TC-001-003 | 测试获取K线数据 | 网络连接正常 | 1. 调用getKLine("000001", startDate, endDate)<br>2. 验证返回数据 | 1. 返回非空列表<br>2. 数据包含OHLCV字段<br>3. 日期在范围内 | P0 |
| TC-001-004 | 测试获取实时价格 | 网络连接正常 | 1. 调用getQuote("000001")<br>2. 验证返回结果 | 1. 返回价格>0<br>2. 包含股票代码和名称 | P0 |
| TC-001-005 | 测试重试机制 | 模拟前两次失败 | 1. 模拟API前2次失败<br>2. 第3次成功<br>3. 调用方法 | 1. 重试3次<br>2. 最终返回成功结果<br>3. 延迟递增 | P1 |

### 2.2 StockDataServiceImplTest - 数据服务测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-001-006 | 测试获取可交易股票列表 | 数据库有数据 | 1. 调用fetchStockList()<br>2. 检查返回结果 | 1. 返回非空列表<br>2. 过滤ST股票<br>3. isTradable=true | P0 |
| TC-001-007 | 测试获取历史数据-从数据库 | 数据库有历史数据 | 1. 调用fetchHistoricalData("000001", 30)<br>2. 验证查询结果 | 1. 从数据库读取<br>2. 返回30条记录<br>3. 不调用API | P0 |
| TC-001-008 | 测试获取历史数据-从API | 数据库无数据 | 1. 清空测试股票数据<br>2. 调用fetchHistoricalData()<br>3. 验证结果 | 1. 调用AKTools API<br>2. 数据写入MongoDB<br>3. 返回数据 | P0 |
| TC-001-009 | 测试获取实时价格-缓存命中 | Redis有缓存 | 1. 设置缓存<br>2. 调用fetchRealTimePrice()<br>3. 验证 | 1. 从Redis读取<br>2. 返回缓存价格<br>3. 不调用API | P0 |
| TC-001-010 | 测试获取实时价格-缓存未命中 | Redis无缓存 | 1. 清除缓存<br>2. 调用fetchRealTimePrice()<br>3. 验证 | 1. 调用API<br>2. 写入Redis缓存<br>3. TTL=60秒 | P0 |
| TC-001-011 | 测试同步单只股票历史 | 网络正常 | 1. 调用syncStockHistory("000001", 60)<br>2. 检查数据库 | 1. 调用API获取数据<br>2. 写入MongoDB<br>3. 无异常抛出 | P0 |
| TC-001-012 | 测试全量同步 | 网络正常 | 1. 调用syncAllStocks()<br>2. 检查MySQL和MongoDB | 1. 同步股票列表到MySQL<br>2. 同步历史数据到MongoDB<br>3. 记录同步日志 | P0 |
| TC-001-013 | 测试重复数据处理 | 数据库已有数据 | 1. 插入测试数据<br>2. 再次同步相同数据<br>3. 验证 | 1. 数据更新而非重复插入<br>2. 数据一致性保持 | P1 |
| TC-001-014 | 测试异常处理 | 模拟异常 | 1. 模拟数据库连接失败<br>2. 调用同步方法<br>3. 验证 | 1. 捕获异常<br>2. 记录错误日志<br>3. 不崩溃 | P1 |

### 2.3 StockInfoMapperTest - 数据访问层测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-001-015 | 测试插入股票信息 | 无 | 1. 创建StockInfo对象<br>2. 调用insert()<br>3. 查询验证 | 1. 插入成功<br>2. 返回影响行数1<br>3. ID自动生成 | P0 |
| TC-001-016 | 测试根据代码查询 | 数据已存在 | 1. 插入测试数据<br>2. 调用getByCode("000001")<br>3. 验证 | 1. 返回正确对象<br>2. code匹配<br>3. 其他字段正确 | P0 |
| TC-001-017 | 测试更新股票信息 | 数据已存在 | 1. 插入数据<br>2. 修改字段<br>3. 调用updateById()<br>4. 验证 | 1. 更新成功<br>2. 字段值已改变<br>3. updateTime更新 | P0 |
| TC-001-018 | 测试删除股票 | 数据已存在 | 1. 插入数据<br>2. 调用deleteByCode()<br>3. 验证 | 1. 删除成功<br>2. 查询返回null | P0 |
| TC-001-019 | 测试查询不存在的股票 | 无 | 1. 调用getByCode("999999")<br>2. 验证 | 1. 返回null<br>2. 无异常 | P0 |
| TC-001-020 | 测试批量插入 | 无 | 1. 创建100条数据<br>2. 批量插入<br>3. 验证 | 1. 全部插入成功<br>2. 性能达标(<1秒) | P1 |

### 2.4 StockPricesRepositoryTest - MongoDB仓库测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-001-021 | 测试保存价格数据 | MongoDB连接正常 | 1. 创建StockPrices<br>2. 调用save()<br>3. 验证 | 1. 保存成功<br>2. ID自动生成 | P0 |
| TC-001-022 | 测试根据代码查询 | 数据已存在 | 1. 插入测试数据<br>2. 调用findByCode()<br>3. 验证 | 1. 返回列表<br>2. 所有记录code匹配 | P0 |
| TC-001-023 | 测试根据代码和日期查询 | 数据已存在 | 1. 插入数据<br>2. 调用findByCodeAndDate()<br>3. 验证 | 1. 返回Optional对象<br>2. 日期匹配 | P0 |
| TC-001-024 | 测试日期范围查询 | 数据已存在 | 1. 插入多日数据<br>2. 调用findByCodeAndDateBetween()<br>3. 验证 | 1. 返回范围内数据<br>2. 日期排序正确 | P0 |
| TC-001-025 | 测试删除价格数据 | 数据已存在 | 1. 插入数据<br>2. 调用deleteByCode()<br>3. 验证 | 1. 删除成功<br>2. 查询返回空 | P0 |
| TC-001-026 | 测试存在性检查 | 数据已存在 | 1. 插入数据<br>2. 调用existsByCodeAndDate()<br>3. 验证 | 1. 返回true<br>2. 不存在的返回false | P0 |

---

## 3. 集成测试用例

### 3.1 DataCollectionIntegrationTest

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-001-027 | 测试端到端股票同步 | 应用启动 | 1. 调用fetchStockList()<br>2. 保存到MySQL<br>3. 查询验证 | 1. MySQL中有数据<br>2. 数量>4000<br>3. 字段完整 | P0 |
| TC-001-028 | 测试端到端历史数据同步 | 应用启动 | 1. 调用fetchHistoricalData()<br>2. 保存到MongoDB<br>3. 查询验证 | 1. MongoDB中有数据<br>2. 数据格式正确<br>3. 索引生效 | P0 |
| TC-001-029 | 测试缓存一致性 | 应用启动 | 1. 获取实时价格<br>2. 修改缓存<br>3. 再次获取 | 1. 首次从API<br>2. 第二次从缓存<br>3. 缓存TTL正确 | P0 |
| TC-001-030 | 测试数据库事务 | 应用启动 | 1. 开始同步<br>2. 模拟中间失败<br>3. 验证数据一致性 | 1. 事务回滚<br>2. 数据不完整<br>3. 无脏数据 | P1 |

---

## 4. 性能测试用例

| 用例ID | 用例名称 | 测试场景 | 预期性能 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-001-P001 | 股票列表获取性能 | 获取全部股票 | < 3秒 | P1 |
| TC-001-P002 | 历史数据查询性能 | 查询1年数据 | < 2秒 | P1 |
| TC-001-P003 | 并发测试 | 100并发请求 | 成功率>99% | P1 |
| TC-001-P004 | 内存使用测试 | 批量处理1000条 | 内存<500MB | P2 |

---

## 5. 测试执行检查清单

### 5.1 前置条件
- [ ] MySQL数据库可连接
- [ ] MongoDB数据库可连接
- [ ] Redis缓存可连接
- [ ] AKTools API可访问（或已mock）
- [ ] 测试数据已清理

### 5.2 执行顺序
1. 单元测试（按类顺序执行）
2. 集成测试
3. 性能测试

### 5.3 通过标准
- 单元测试通过率: 100%
- 集成测试通过率: 100%
- 代码覆盖率: > 70%
- 无严重BUG

---

## 6. BUG记录模板

| BUG ID | 描述 | 严重级别 | 状态 | 修复方案 | 验证结果 |
|--------|------|---------|------|---------|---------|
| BUG-001 | | | | | |

---

## 7. 测试报告

### 7.1 执行摘要
- 总用例数: 
- 通过数: 
- 失败数: 
- 跳过数: 
- 代码覆盖率: 

### 7.2 遗留问题
- 

### 7.3 结论
- 
