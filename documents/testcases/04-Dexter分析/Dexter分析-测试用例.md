# 模块四：Dexter分析 - 测试用例文档

## 文档信息
- **模块**: MOD-004 Dexter分析
- **版本**: 2.0
- **创建日期**: 2026-02-19
- **测试类型**: 真实数据库集成测试 + 数据流穿透测试

---

## 1. 测试范围

### 1.1 测试目标
验证Dexter分析模块的所有功能是否按需求正常工作，包括：
- Dexter API调用
- 股票基本面分析
- 实时报价获取
- **分析结果真实写入 Redis 缓存**
- **缓存一致性验证**

### 1.2 测试环境
- **JDK**: 17
- **测试框架**: Spring Boot Test + JUnit 5
- **缓存**: Redis (Embedded)
- **数据管理**: Database Rider

### 1.3 测试原则
| 原则 | 说明 |
|------|------|
| 真实性 | 使用真实 Redis，验证缓存真实写入 |
| 隔离性 | 测试完成后自动清理缓存 |
| 可追溯 | 每个步骤都可验证缓存状态 |
| 全链路 | 从 API 到缓存完整穿透 |

---

## 2. 数据流测试用例

### 2.1 Dexter分析数据流测试

```
数据流: Dexter API → DexterService → Redis Cache (分析结果)
验证点:     V1            V2              V3
```

| 用例ID | 用例名称 | 测试步骤 | 验证点 | 缓存验证 | 预期结果 | 优先级 |
|--------|---------|---------|--------|---------|---------|--------|
| TC-004-F001 | 分析结果缓存流程 | 1. 调用analyze()<br>2. 结果写入Redis<br>3. 验证缓存 | V1-V3 | `GET dexter:analysis:000001` | 1. 分析成功<br>2. 缓存写入<br>3. TTL=1小时 | P0 |
| TC-004-F002 | 缓存命中流程 | 1. 首次调用写入缓存<br>2. 再次调用<br>3. 验证来源 | V3 | 响应时间<100ms | 1. 从缓存读取<br>2. 不调用API | P0 |

### 2.2 Redis 缓存验证示例

```java
// 验证缓存写入
String cachedAnalysis = redisTemplate.opsForValue().get("dexter:analysis:000001");
assertThat(cachedAnalysis).isNotNull();

// 验证 TTL
Long ttl = redisTemplate.getExpire("dexter:analysis:000001", TimeUnit.SECONDS);
assertThat(ttl).isGreaterThan(0);
assertThat(ttl).isLessThanOrEqualTo(3600);  // 1小时

// 验证缓存内容
DexterResult result = JSON.parseObject(cachedAnalysis, DexterResult.class);
assertThat(result.getFinancialScore()).isBetween(0, 100);
```

---

## 2. 单元测试用例

### 2.1 DexterServiceImplTest - Dexter服务测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-001 | 测试获取基本面分析 | Dexter服务正常 | 1. 调用analyze("000001")<br>2. 验证返回结果 | 1. 返回DexterResult<br>2. 包含financialScore<br>3. 包含suggestion | P0 |
| TC-004-002 | 测试获取实时报价 | Dexter服务正常 | 1. 调用getQuote("000001")<br>2. 验证 | 1. 返回currentPrice<br>2. 返回change<br>3. 返回changePercent | P0 |
| TC-004-003 | 测试获取交易建议 | Dexter服务正常 | 1. 调用getSuggestion("000001")<br>2. 验证 | 1. 返回Suggestion对象<br>2. 包含buy/hold/sell<br>3. 包含targetPrice | P0 |
| TC-004-004 | 测试缓存命中 | Redis有缓存 | 1. 设置缓存<br>2. 调用analyze()<br>3. 验证 | 1. 从缓存读取<br>2. 不调用API<br>3. 响应<100ms | P0 |
| TC-004-005 | 测试缓存未命中 | Redis无缓存 | 1. 清除缓存<br>2. 调用analyze()<br>3. 验证 | 1. 调用Dexter API<br>2. 写入Redis<br>3. TTL=1小时 | P0 |
| TC-004-006 | 测试API超时处理 | 模拟超时 | 1. 模拟API超时<br>2. 调用analyze() | 1. 30秒超时<br>2. 重试3次<br>3. 返回缓存或默认值 | P0 |
| TC-004-007 | 测试API失败降级 | Dexter不可用 | 1. Dexter服务停止<br>2. 调用analyze()<br>3. 验证降级 | 1. 返回缓存数据<br>2. 或无缓存返回默认值<br>3. 记录警告日志 | P0 |
| TC-004-008 | 测试评分范围验证 | 无 | 1. 调用analyze()<br>2. 验证score范围 | 1. financialScore: 0-100<br>2. growthScore: 0-100<br>3. riskLevel正确 | P0 |
| TC-004-009 | 测试无效股票代码 | 无 | 1. 调用analyze("invalid")<br>2. 验证 | 1. 返回错误<br>2. 或返回空结果<br>3. 记录日志 | P0 |
| TC-004-010 | 测试批量分析 | 无 | 1. 传入10只股票<br>2. 调用batchAnalyze() | 1. 返回10个结果<br>2. 批量API调用<br>3. 提高效率 | P1 |

### 2.2 DexterClientTest - API客户端测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-011 | 测试POST /api/dexter/analyze | Dexter服务启动 | 1. 调用分析接口<br>2. 传入stockCode<br>3. 验证 | 1. HTTP 200<br>2. 返回JSON<br>3. 字段完整 | P0 |
| TC-004-012 | 测试GET /api/dexter/quote/{code} | Dexter服务启动 | 1. 调用报价接口<br>2. 传入code<br>3. 验证 | 1. HTTP 200<br>2. 返回price<br>3. 实时数据 | P0 |
| TC-004-013 | 测试API认证 | 需要认证 | 1. 传入API Key<br>2. 调用接口<br>3. 验证 | 1. 认证通过<br>2. 返回数据<br>3. 错误Key返回401 | P0 |
| TC-004-014 | 测试重试机制 | 模拟失败 | 1. 模拟前2次失败<br>2. 第3次成功<br>3. 验证 | 1. 重试3次<br>2. 最终成功<br>3. 指数退避 | P0 |
| TC-004-015 | 测试连接池 | 并发调用 | 1. 同时发起10请求<br>2. 监控连接池 | 1. 连接复用<br>2. 无连接泄漏<br>3. 性能稳定 | P1 |

### 2.3 FinancialScoreCalculatorTest - 财务评分计算测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-016 | 测试PE计算 | 有财务数据 | 1. 计算市盈率<br>2. 验证公式 | 1. PE=股价/每股收益<br>2. 结果正确 | P0 |
| TC-004-017 | 测试PB计算 | 有财务数据 | 1. 计算市净率<br>2. 验证公式 | 1. PB=股价/每股净资产<br>2. 结果正确 | P0 |
| TC-004-018 | 测试ROE计算 | 有财务数据 | 1. 计算净资产收益率<br>2. 验证 | 1. ROE=净利润/净资产<br>2. 百分比形式 | P0 |
| TC-004-019 | 测试营收增长率 | 有多期数据 | 1. 计算增长率<br>2. 验证 | 1. (本期-上期)/上期<br>2. 同比或环比 | P0 |
| TC-004-020 | 测试综合财务评分 | 有多项指标 | 1. 计算综合评分<br>2. 验证权重 | 1. 范围0-100<br>2. 权重正确<br>3. 公式无误 | P0 |
| TC-004-021 | 测试风险等级判断 | 有评分结果 | 1. 高分→低风险<br>2. 低分→高风险<br>3. 验证 | 1. 等级划分正确<br>2. high/medium/low | P0 |

### 2.4 DexterCacheTest - 缓存管理测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-022 | 测试缓存写入 | Redis连接正常 | 1. 保存分析结果<br>2. 验证Redis | 1. Key格式正确<br>2. Value正确<br>3. TTL设置 | P0 |
| TC-004-023 | 测试缓存读取 | 缓存已存在 | 1. 读取缓存<br>2. 验证数据 | 1. 返回正确对象<br>2. 数据完整<br>3. 反序列化正确 | P0 |
| TC-004-024 | 测试缓存过期 | 缓存即将过期 | 1. 等待TTL过期<br>2. 尝试读取 | 1. 返回null<br>2. 触发API调用 | P0 |
| TC-004-025 | 测试缓存更新 | 缓存已存在 | 1. 更新分析结果<br>2. 重新缓存 | 1. 覆盖旧值<br>2. TTL重置<br>3. 新数据可用 | P0 |
| TC-004-026 | 测试缓存删除 | 缓存已存在 | 1. 删除缓存<br>2. 验证 | 1. 删除成功<br>2. 查询返回null | P0 |
| TC-004-027 | 测试报价缓存TTL | 无 | 1. 保存报价<br>2. 验证TTL | 1. TTL=1分钟<br>2. 自动过期 | P0 |
| TC-004-028 | 测试分析缓存TTL | 无 | 1. 保存分析<br>2. 验证TTL | 1. TTL=1小时<br>2. 自动过期 | P0 |

### 2.5 DexterControllerTest - API接口测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-029 | 测试GET /api/dexter/analyze/{code} | 无 | 1. GET分析接口<br>2. 传入code<br>3. 验证 | 1. HTTP 200<br>2. 返回完整分析<br>3. JSON格式正确 | P0 |
| TC-004-030 | 测试GET /api/dexter/quote/{code} | 无 | 1. GET报价接口<br>2. 传入code<br>3. 验证 | 1. HTTP 200<br>2. 返回currentPrice<br>3. 数据实时 | P0 |
| TC-004-031 | 测试GET /api/dexter/suggestion/{code} | 无 | 1. GET建议接口<br>2. 传入code<br>3. 验证 | 1. HTTP 200<br>2. 返回suggestion<br>3. 建议合理 | P0 |
| TC-004-032 | 测试参数校验-无效代码 | 无 | 1. GET /api/dexter/analyze/invalid<br>2. 验证 | 1. HTTP 400<br>2. 返回错误信息 | P0 |
| TC-004-033 | 测试服务不可用错误 | Dexter故障 | 1. Dexter停止<br>2. 调用接口<br>3. 验证 | 1. HTTP 503或200<br>2. 返回降级数据<br>3. 记录日志 | P0 |
| TC-004-034 | 测试批量分析接口 | 无 | 1. POST批量接口<br>2. 传入代码列表<br>3. 验证 | 1. HTTP 200<br>2. 返回列表<br>3. 结果完整 | P1 |

---

## 3. 集成测试用例

### 3.1 DexterIntegrationTest

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-004-I001 | 测试端到端分析流程 | 全链路启动 | 1. Java调用DexterClient<br>2. Dexter API处理<br>3. 返回分析结果 | 1. 流程完成<br>2. 结果正确<br>3. 字段完整 | P0 |
| TC-004-I002 | 测试缓存一致性 | 全链路启动 | 1. 首次调用（无缓存）<br>2. 验证API调用<br>3. 再次调用 | 1. 首次走API<br>2. 第二次走缓存<br>3. 响应<100ms | P0 |
| TC-004-I003 | 测试API失败降级 | Dexter不可用 | 1. 停止Dexter<br>2. 调用分析<br>3. 验证降级 | 1. 返回缓存或默认<br>2. 系统不崩溃<br>3. 记录警告 | P0 |
| TC-004-I004 | 测试性能达标 | 全链路启动 | 1. 测试API调用时间<br>2. 测试缓存响应 | 1. API<5s<br>2. 缓存<100ms | P0 |
| TC-004-I005 | 测试并发请求 | 全链路启动 | 1. 同时发起10请求<br>2. 监控响应 | 1. 全部成功<br>2. 无超时<br>3. 无错误 | P1 |

---

## 4. 性能测试用例

| 用例ID | 用例名称 | 测试场景 | 预期性能 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-004-P001 | API响应时间 | 单次分析调用 | < 5s | P0 |
| TC-004-P002 | 缓存响应时间 | 缓存命中场景 | < 100ms | P0 |
| TC-004-P003 | 批量分析时间 | 10只股票批量 | < 30s | P1 |
| TC-004-P004 | 并发处理能力 | 10并发请求 | 成功率>95% | P1 |
| TC-004-P005 | Redis缓存性能 | 读写操作 | < 10ms | P1 |

---

## 5. 测试执行检查清单

### 5.1 前置条件
- [ ] Dexter服务已部署（本地或云端）
- [ ] API Key已配置
- [ ] Redis服务已启动
- [ ] Java后端已编译
- [ ] 网络可访问Dexter服务

### 5.2 执行顺序
1. 单元测试（按类顺序执行）
2. 集成测试
3. 性能测试

### 5.3 通过标准
- 单元测试通过率: 100%
- 集成测试通过率: 100%
- 代码覆盖率: > 70%
- API响应<5s
- 缓存响应<100ms
- 无严重BUG

---

## 6. BUG记录模板

| BUG ID | 描述 | 严重级别 | 状态 | 修复方案 | 验证结果 |
|--------|------|---------|------|---------|---------|
| BUG-001 | | | | | |

---

## 7. 测试报告

### 7.1 执行摘要
- 总用例数: 
- 通过数: 
- 失败数: 
- 跳过数: 
- 代码覆盖率: 

### 7.2 遗留问题
- 

### 7.3 结论
- 
