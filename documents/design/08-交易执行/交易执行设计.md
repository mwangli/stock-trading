# 交易执行模块设计

## 1. 文档信息

| 属性 | 内容 |
|------|------|
| 模块ID | MOD-008 |
| 模块名称 | 交易执行 |
| 版本 | 1.0 |
| 状态 | 已完成 |
| 创建日期 | 2026-02-17 |
| 最后更新 | 2026-02-17 |
| 负责人 | 开发团队 |
| 作者 | mwangli |

## 2. 概述

### 2.1 模块定位

交易执行模块是股票交易系统的执行层，负责对接券商API，执行买入/卖出订单，管理订单状态，处理持仓和资金，并接收券商回调通知。

### 2.2 功能摘要

- 买入/卖出委托执行
- 订单状态管理
- 持仓管理
- 资金管理
- 券商回调处理

### 2.3 上下游依赖

| 上游模块 | 接口 | 数据 |
|----------|------|------|
| 决策引擎 | executeTrade() | 交易信号 |
| 风控管理 | checkBeforeTrade() | 风控检查结果 |

| 下游模块 | 接口 | 数据 |
|----------|------|------|
| 券商API | - | 订单执行 |

## 3. 功能需求

### 3.1 核心功能列表

| 功能ID | 功能名称 | 功能描述 | 优先级 |
|--------|----------|----------|--------|
| F-001 | 买入委托 | 执行买入订单 | P0 |
| F-002 | 卖出委托 | 执行卖出订单 | P0 |
| F-003 | 撤单 | 撤销未成交订单 | P0 |
| F-004 | 订单查询 | 查询订单状态 | P0 |
| F-005 | 持仓管理 | 管理当前持仓 | P0 |
| F-006 | 回调处理 | 处理券商回调 | P0 |

### 3.2 功能详情

#### F-001: 买入委托

**功能描述**: 执行买入订单，包括风控检查、资金检查、发送委托。

**输入**: BuyOrderRequest

**处理逻辑**:
1. 风控检查
2. 计算买入金额
3. 检查资金余额
4. 发送委托
5. 创建订单记录

**输出**: OrderResult

**业务规则**:
- 限价委托：指定价格成交
- 市价委托：以最优价格成交

#### F-002: 卖出委托

**功能描述**: 执行卖出订单，包括持仓检查、T+1检查、风控检查。

**输入**: SellOrderRequest

**处理逻辑**:
1. 获取持仓
2. T+1检查
3. 风控检查
4. 发送委托
5. 冻结持仓

**输出**: OrderResult

**业务规则**:
- T+1限制：当日买入，次日出
- 冻结持仓数量

## 4. 非功能需求

### 4.1 性能要求

| 指标 | 要求 | 说明 |
|------|------|------|
| 委托响应 | < 3s | 券商接口响应 |
| 状态查询 | < 1s | 订单状态查询 |
| 持仓更新 | < 100ms | 持仓变更 |

### 4.2 可用性要求

- 委托失败自动重试3次
- 超时后查询确认状态
- 服务SLA: 99.9%

## 5. 总体设计

### 5.1 技术架构

| 组件 | 技术选型 | 版本 |
|------|---------|------|
| 框架 | Spring Boot | 2.6.x |
| 数据访问 | MyBatis-Plus | 3.5.x |
| 任务调度 | Quartz | 2.3.x |

### 5.2 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      Spring Boot Backend                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                  Service Layer                            │   │
│  │  ┌─────────────────────────────────────────────────┐    │   │
│  │  │         TradingExecutionService                    │    │   │
│  │  │  - executeBuy()         买入委托               │    │   │
│  │  │  - executeSell()        卖出委托               │    │   │
│  │  │  - cancelOrder()       撤单                   │    │   │
│  │  │  - queryOrderStatus()  查询状态               │    │   │
│  │  │  - handleCallback()    回调处理               │    │   │
│  │  └─────────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────▼──────────────────────────┐        │
│  │                   Order Management                   │        │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────┐  │        │
│  │  │ OrderManager │ │ PositionMgr  │ │ProfitTracker│  │        │
│  │  │  订单状态管理 │ │   持仓管理   │ │  收益追踪 │  │        │
│  │  └──────────────┘ └──────────────┘ └──────────┘  │        │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────▼──────────────────────────┐        │
│  │                Broker Integration                   │        │
│  │  ┌──────────────────────────────────────────┐   │        │
│  │  │        BrokerAPI (中信证券等)               │   │        │
│  │  │  - login()           登录                │   │        │
│  │  │  - sendOrder()      委托                │   │        │
│  │  │  - cancelOrder()    撤单                │   │        │
│  │  │  - queryBalance()   查询余额            │   │        │
│  │  │  - queryPositions() 查询持仓            │   │        │
│  │  └──────────────────────────────────────────┘   │        │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 部署架构

- 部署方式: Spring Boot应用
- 依赖: MySQL
- 定时任务: Quartz调度

## 6. 详细设计

### 6.1 订单状态机

```
创建 (CREATE)
    │
    ▼
提交 (SUBMIT) ─── 失败 (FAILED)
    │
    ▼
待确认 (PENDING) ── 拒绝 (REJECTED)
    │
    ▼
已确认 (CONFIRMED)
    │
    ├──────┬──────────┬──────────┐
    ▼      ▼          ▼          ▼
部分成交 全部成交  部分撤单   全部撤单
(PARTIAL) (FILLED)  (PARTIAL_CANCEL) (CANCELLED)
    │      │          │            │
    └──────┴──────────┴────────────┘
                    │
                    ▼
                 终态 (FINAL)
```

### 6.2 买入委托流程

```java
public OrderResult executeBuy(BuyOrderRequest request) {
    // 1. 风控检查
    RiskCheckResult riskCheck = riskService.check(request);
    if (!riskCheck.isPassed()) {
        return OrderResult.fail(riskCheck.getRejectReason());
    }
    
    // 2. 计算买入金额
    BigDecimal amount = request.getPrice().multiply(request.getQuantity());
    
    // 3. 检查资金余额
    BigDecimal available = accountService.getAvailableBalance();
    if (available.compareTo(amount) < 0) {
        return OrderResult.fail("资金不足");
    }
    
    // 4. 发送委托
    String orderId = brokerAPI.sendOrder(request);
    
    // 5. 创建订单记录
    Order order = createOrderRecord(request, orderId);
    
    // 6. 返回结果
    return OrderResult.success(order);
}
```

### 6.3 卖出委托流程

```java
public OrderResult executeSell(SellOrderRequest request) {
    // 1. 获取持仓
    Position position = positionService.getById(request.getPositionId());
    
    // 2. T+1 检查
    if (!t1Strategy.canSell(position)) {
        return OrderResult.fail("T+1 限制，次日才能卖出");
    }
    
    // 3. 风控检查
    RiskCheckResult riskCheck = riskService.check(request);
    if (!riskCheck.isPassed()) {
        return OrderResult.fail(riskCheck.getRejectReason());
    }
    
    // 4. 发送委托
    String orderId = brokerAPI.sendOrder(request);
    
    // 5. 创建订单记录
    Order order = createOrderRecord(request, orderId);
    
    // 6. 更新持仓 (冻结部分)
    positionService.freeze(position, request.getQuantity());
    
    return OrderResult.success(order);
}
```

### 6.4 数据模型设计

#### 6.4.1 订单

```java
@Data
@Entity
public class Order {
    @Id
    private String orderId;             // 券商订单ID
    
    private String stockCode;           // 股票代码
    private String stockName;          // 股票名称
    
    private OrderDirection direction;   // BUY / SELL
    private OrderType type;            // LIMIT / MARKET
    
    private BigDecimal price;          // 委托价格
    private Integer quantity;          // 委托数量
    private Integer filledQuantity;     // 成交数量
    
    private OrderStatus status;        // 订单状态
    private String message;            // 错误信息
    
    private String positionId;         // 关联持仓ID
    
    private LocalDateTime createdAt;   // 创建时间
    private LocalDateTime updatedAt;   // 更新时间
    private LocalDateTime filledAt;    // 成交时间
}
```

#### 6.4.2 持仓

```java
@Data
@Entity
public class Position {
    @Id
    private String id;
    
    private String stockCode;
    private String stockName;
    
    private Integer quantity;          // 持仓数量
    private BigDecimal costPrice;     // 成本价
    private BigDecimal currentPrice;  // 当前价
    
    private Integer frozenQuantity;   // 冻结数量 (待卖出)
    
    private LocalDate buyDate;        // 买入日期 (T+1用)
    
    private BigDecimal profit;        // 持仓盈亏
    private BigDecimal profitPercent; // 盈亏比例
    
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
```

### 6.5 订单类型

| 类型 | 说明 | 特点 |
|------|------|------|
| LIMIT | 限价委托 | 指定价格成交 |
| MARKET | 市价委托 | 以最优价格成交 |
| STOP | 止损委托 | 触发后以市价成交 |

### 6.6 回调处理

```java
@PostMapping("/api/broker/callback")
public void handleBrokerCallback(@RequestBody BrokerCallback callback) {
    // 1. 根据订单ID更新订单状态
    Order order = orderRepository.findByOrderId(callback.getOrderId());
    
    // 2. 处理不同状态
    switch (callback.getStatus()) {
        case FILLED:
            handleFilled(order, callback);
            break;
        case PARTIAL_FILLED:
            handlePartialFilled(order, callback);
            break;
        case CANCELLED:
            handleCancelled(order, callback);
            break;
        case REJECTED:
            handleRejected(order, callback);
            break;
    }
}
```

### 6.7 配置设计

```yaml
# 交易执行配置
trading:
  execution:
    # 重试配置
    retry:
      max-attempts: 3
      delay: 1000  # ms
      
    # 超时配置
    timeout:
      order: 30000   # 30秒
      query: 5000    # 5秒
      
    # 订单配置
    order:
      default-type: LIMIT  # 默认限价
      max-quantity: 10000  # 单笔最大数量
```

## 7. 错误处理

### 7.1 错误码定义

| 错误码 | 说明 | 处理策略 |
|--------|------|----------|
| E001 | 资金不足 | 降低数量或取消 |
| E002 | 股票停牌 | 取消订单 |
| E003 | 涨跌停 | 调整价格重试 |
| E004 | 数量不足 | 调整数量 |
| E005 | 委托失败 | 重试3次 |
| E006 | 超时 | 查询状态确认 |

### 7.2 异常处理策略

| 异常类型 | 处理策略 |
|----------|----------|
| 资金不足 | 返回失败，建议调整数量 |
| 涨跌停 | 返回失败，提示价格 |
| 委托失败 | 重试3次，失败后查询状态 |
| 超时 | 查询订单状态确认 |

### 7.3 重试机制

```java
public String sendOrderWithRetry(OrderRequest request) {
    int maxRetries = 3;
    int retryDelay = 1000; // ms
    
    for (int i = 0; i < maxRetries; i++) {
        try {
            return brokerAPI.sendOrder(request);
        } catch (Exception e) {
            if (i == maxRetries - 1) {
                throw new TradingException("委托失败: " + e.getMessage());
            }
            Thread.sleep(retryDelay * (i + 1));
        }
    }
}
```

## 8. 监控与运维

### 8.1 监控指标

| 指标 | 告警阈值 | 说明 |
|------|----------|------|
| 委托成功率 | < 95% | 委托成功率 |
| 委托耗时 | > 5s | 委托响应时间 |
| 失败订单数 | > 10 | 失败订单数量 |

### 8.2 日志设计

- 委托日志
- 回调日志
- 错误堆栈日志

### 8.3 缓存策略

| 数据 | 缓存方式 | 过期时间 | 说明 |
|------|----------|----------|------|
| 持仓数据 | Redis | 实时 | 持仓信息 |
| 订单状态 | MySQL | 长期 | 订单记录 |

## 9. 任务清单

| 任务ID | 任务名称 | 优先级 | 依赖 | 预估工时 | 状态 |
|--------|----------|--------|------|----------|------|
| T-001 | TradeExecutionService接口定义 | P0 | - | 0.5h | 待开始 |
| T-002 | 券商API适配器 | P0 | T-001 | 2h | 待开始 |
| T-003 | 订单管理服务 | P0 | T-002 | 1.5h | 待开始 |
| T-004 | 定时交易任务 | P0 | T-003 | 1h | 待开始 |
| T-005 | 止损卖出服务 | P0 | T-003 | 1h | 待开始 |

### 9.1 任务详情

#### T-001: TradeExecutionService接口定义

**文件路径**: `stock-backend/.../service/TradeExecutionService.java`

**验收标准**:
- [ ] 接口方法签名正确
- [ ] 方法注释完整

#### T-002: 券商API适配器

**文件路径**: `stock-backend/.../adapter/BrokerAdapter.java`

**验收标准**:
- [ ] 下单接口可调用
- [ ] 订单状态可查询
- [ ] 持仓信息可获取

#### T-003: 订单管理服务

**文件路径**: `stock-backend/.../service/impl/OrderService.java`

**验收标准**:
- [ ] 订单状态正确
- [ ] 交易记录正确
- [ ] 异常订单处理正确

### 执行顺序

```
T-001 (接口) ──▶ T-002 (适配器) ──▶ T-003 (订单管理) ──┬──▶ T-004 (定时任务)
                                                       └──▶ T-005 (止损卖出)
```

## 10. 验收标准

### 10.1 功能验收

- [ ] 买入委托执行成功
- [ ] 卖出委托执行成功
- [ ] 撤单功能正常
- [ ] 持仓管理正确

### 10.2 性能验收

- [ ] 委托响应 < 3秒
- [ ] 状态查询 < 1秒
- [ ] 持仓更新 < 100ms

### 10.3 可用性验收

- [ ] 委托失败自动重试
- [ ] 超时后查询确认
- [ ] 回调处理正常

---

*文档版本: 1.0*
*最后更新: 2026-02-17*
*作者: mwangli*
