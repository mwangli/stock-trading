# PRD-001: 商品数据采集系统需求文档

## 基本信息
- **文档编号**: PRD-001
- **版本**: v3.0
- **状态**: 已更新
- **作者**: mwangli
- **最后更新**: 2026-02-16

## 变更日志
| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| v3.0 | 2026-02-16 | mwangli | 采集功能改为Java实现（Spring Boot + Jsoup） |
| v2.0 | 2026-02-16 | mwangli | 重构为商品数据采集系统，增加数据库持久化和定时任务 |
| v1.0 | 2026-02-16 | mwangli | 初始版本 |

---

## 1. 需求概述

### 1.1 项目背景
本项目是一个针对南非电商平台Takealot.com的数据采集和处理系统。主要目的是为私域电商运营提供高质量的产品数据源，通过Java后端服务自动化采集商品信息，并持久化存储到数据库中，同时提供API接口供前端展示。

### 1.2 项目目标
- 使用Java技术栈自动化采集Takealot.com平台的商品数据
- 建立品牌白名单/黑名单机制，规避知名品牌侵权风险
- **将采集数据持久化存储到数据库中**
- **提供REST API接口供前端查询和展示**
- **支持定时自动采集任务（Spring Scheduler）**
- 支持多分类、多品牌的批量采集

### 1.3 业务范围
**包含**:
- 商品基础信息采集（标题、价格、图片、描述等）
- 商品分类信息爬取
- 品牌过滤和筛选
- **数据持久化到数据库**
- **定时自动采集任务**
- **REST API接口提供**
- 代理IP管理和切换

**不包含**:
- 商品库存实时监控
- 价格变动追踪
- 订单管理功能
- 支付集成
- 供应商直接对接

---

## 2. 功能需求

### 2.1 功能模块列表

#### 2.1.1 分类信息采集
- [ ] 自动获取Takealot.com所有主分类信息
- [ ] 支持子分类递归采集
- [ ] **分类数据持久化到数据库**
- [ ] 分类URL映射和导航

**业务规则**:
- 分类名称需清洗，排除"Home"、"Cart"、"Search"等非商品分类
- 分类层级不超过3级
- 分类数据需包含URL链接

#### 2.1.2 商品数据采集
- [ ] 按分类批量采集商品数据
- [ ] 支持TOP N商品采集（如前10名）
- [ ] 商品详情页深度爬取
- [ ] 多字段信息采集
- [ ] **数据写入数据库，包含采集时间字段**

**采集字段清单**:
| 字段名 | 英文名称 | 必填 | 说明 |
|--------|----------|------|------|
| 商品页面URL | product_url | 是 | Takealot商品详情页链接 |
| TSIN码 | tsin | 是 | Takealot唯一商品标识 |
| 主分类 | main_category | 是 | 商品所属主分类 |
| 最低分类 | lowest_category | 是 | 商品所属最细粒度分类 |
| 商品标题 | product_title | 是 | 商品名称 |
| 副标题 | subtitle | 否 | 商品副标题/短描述 |
| 商品描述 | description | 是 | 详细商品描述 |
| 包装清单 | whats_in_box | 否 | 包装内含物 |
| 品牌 | brand | 是 | 商品品牌 |
| 保修类型 | warranty_type | 否 | 保修服务类型 |
| 保修期限 | warranty_period | 否 | 保修时长（月） |
| 商品图片 | image_urls | 是 | JSON数组，最多5张图片URL |
| 评分 | rating | 否 | 用户评分（1-5星） |
| 评论数 | review_count | 否 | 用户评论数量 |
| 价格 | price | 是 | 商品售价 |
| 视频链接 | video_url | 否 | YouTube视频URL |
| 采集时间 | collected_at | 是 | 数据采集时间 |
| 采集任务ID | task_id | 是 | 关联采集任务 |

#### 2.1.3 品牌过滤系统
- [ ] 知名品牌黑名单维护
- [ ] 自动品牌识别和过滤
- [ ] 品牌名称模糊匹配
- [ ] 过滤统计和日志记录

**品牌黑名单（部分）**:
- **手机/电子产品**: Apple, Samsung, Huawei, Xiaomi, Google, Sony, LG等
- **电脑/笔记本**: Dell, HP, Acer, MSI, Microsoft, MacBook, ThinkPad等
- **相机**: Canon, Nikon, Fujifilm, GoPro, DJI, Panasonic等
- **音频**: Beats, Bose, JBL, Sennheiser, AirPods等
- **游戏**: PlayStation, Xbox, Nintendo, Logitech, Razer等
- **服装/时尚**: Nike, Adidas, Puma, Gucci, Prada, Zara, H&M等
- **手表/珠宝**: Rolex, Omega, Cartier, Tiffany, Casio等
- **其他**: LEGO, Disney, Coca-Cola, Nestle等国际知名品牌

#### 2.1.4 数据持久化
- [ ] **商品数据写入数据库**
- [ ] **支持重复数据检测和更新**
- [ ] **采集历史记录保存**
- [ ] **数据库表结构自动创建**

**数据库表结构**:
- 商品表 (products)
- 分类表 (categories)
- 采集任务表 (collection_tasks)
- 采集日志表 (collection_logs)
- 品牌黑名单表 (brand_blacklist)

#### 2.1.5 API接口服务
- [ ] **商品列表查询接口**
- [ ] **商品详情查询接口**
- [ ] **分类列表查询接口**
- [ ] **采集任务状态查询接口**
- [ ] **支持分页和筛选**

**接口清单**:
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 商品列表 | GET | /api/products | 分页查询商品列表，支持按分类、品牌筛选 |
| 商品详情 | GET | /api/products/{id} | 查询单个商品详情 |
| 分类列表 | GET | /api/categories | 获取所有商品分类 |
| 采集任务列表 | GET | /api/collection-tasks | 获取采集任务列表 |
| 启动采集任务 | POST | /api/collection-tasks/{id}/execute | 手动启动采集任务 |
| 任务详情 | GET | /api/collection-tasks/{id} | 查询任务执行详情 |

#### 2.1.6 定时自动采集
- [ ] **支持配置定时采集规则（Cron表达式）**
- [ ] **自动触发采集任务（Spring Scheduler）**
- [ ] **任务执行状态监控**
- [ ] **异常告警和重试机制**

**定时任务配置**:
- 支持Cron表达式配置
- 支持按分类分别配置采集频率
- 支持设置每次采集数量限制
- 支持采集时间段限制

#### 2.1.7 代理和并发管理
- [ ] 代理IP配置管理
- [ ] 自动代理切换和故障转移
- [ ] 多线程并发采集（@Async）
- [ ] 采集进度保存和恢复

### 2.2 用户故事

#### 故事1: 运营人员查看商品列表
**作为** 电商运营人员  
**我希望** 通过前端页面查看已采集的商品列表  
**以便** 了解当前产品库存和分布  

**验收标准**:
- 页面展示商品图片、标题、价格、品牌等信息
- 支持按分类、品牌筛选
- 支持分页显示
- 支持关键词搜索

#### 故事2: 管理员启动采集任务
**作为** 系统管理员  
**我希望** 手动或自动触发商品采集任务  
**以便** 定期更新商品数据  

**验收标准**:
- 支持手动触发采集任务
- 支持配置定时自动采集
- 任务执行过程可监控
- 采集结果写入数据库

#### 故事3: 开发人员调用API获取数据
**作为** 前端开发人员  
**我希望** 通过REST API获取商品数据  
**以便** 在页面展示给用户  

**验收标准**:
- API返回JSON格式数据
- 支持分页参数
- 响应时间 < 500ms
- 包含完整的商品信息

---

## 3. 非功能需求

### 3.1 性能要求
- API响应时间 < 500ms（95th percentile）
- 单页面加载时间 < 2秒
- 单个商品信息采集时间 < 5秒
- 并发线程数支持 3-5个（使用@Async线程池）
- 数据库查询优化，支持100万+商品数据

### 3.2 稳定性要求
- 支持代理IP自动切换
- 采集失败自动重试（最多3次）
- 异常数据跳过，不影响整体流程
- HTTP连接池配置合理（使用HttpClient）
- 数据库连接池配置合理

### 3.3 数据质量要求
- 品牌过滤准确率 > 95%
- 字段完整性 > 90%
- 图片URL有效性检查
- 重复数据自动去重（基于TSIN）
- 采集时间准确记录

### 3.4 安全要求
- API接口需要认证（JWT Token）
- 遵守目标网站的robots.txt协议
- 请求频率控制，避免触发反爬
- 用户代理随机轮换
- 敏感信息（代理配置、数据库密码）配置化存储

---

## 4. 系统架构

### 4.1 技术栈

#### 后端/采集层（纯Java实现）
| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.0.x | 应用框架 |
| Java | 17 | 开发语言 |
| Spring Scheduler | 3.0.x | 定时任务 |
| Jsoup | 1.16.x | HTML解析 |
| HttpClient | JDK内置 | HTTP请求 |
| MySQL | 8.0 | 关系型数据库 |
| MyBatis-Plus | 3.5.5 | ORM框架 |
| Maven | 3.6.2 | 构建工具 |

#### 前端层
| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.x | UI框架 |
| Ant Design Pro | 5.x | UI组件库 |
| TypeScript | 5.x | 类型安全 |

### 4.2 系统模块

```
商品数据采集系统（Java实现）
├── 后端服务 (Spring Boot)
│   ├── controller/                 # REST API控制器
│   │   ├── ProductController            # 商品API
│   │   ├── CategoryController           # 分类API
│   │   └── CollectionTaskController     # 采集任务API
│   ├── service/                    # 业务逻辑层 (extends ServiceImpl<M, T>)
│   │   ├── ProductService          # extends IService<Product>
│   │   ├── CategoryService         # extends IService<Category>
│   │   ├── CollectionTaskService   # extends IService<CollectionTask>
│   │   └── collector/              # 采集服务层
│   │       ├── ProductCollectorService     # 商品采集核心服务
│   │       ├── TakealotCollector           # Takealot采集器
│   │       ├── BrandFilterService          # 品牌过滤服务
│   │       └── HttpClientService           # HTTP请求服务
│   ├── mapper/                     # MyBatis-Plus数据访问层 (extends BaseMapper)
│   │   ├── ProductMapper
│   │   ├── CategoryMapper
│   │   ├── CollectionTaskMapper
│   │   └── BrandBlacklistMapper
│   ├── entity/                     # MyBatis-Plus实体类 (@TableName)
│   │   ├── Product
│   │   ├── Category
│   │   ├── CollectionTask
│   │   ├── CollectionLog
│   │   └── BrandBlacklist
│   ├── scheduler/                  # 定时任务
│   │   └── CollectionScheduler
│   ├── config/                     # 配置类
│   │   ├── AsyncConfig             # 异步线程池配置
│   │   ├── HttpClientConfig        # HTTP客户端配置
│   │   └── MyBatisPlusConfig       # MyBatis-Plus配置
│   └── dto/                        # 数据传输对象
│
└── 前端展示 (React)
    ├── pages/
    │   ├── ProductList             # 商品列表页
    │   ├── ProductDetail           # 商品详情页
    │   └── CollectionTasks         # 采集任务管理页
    └── services/
        └── productApi.ts           # API调用
```

### 4.3 采集服务设计

**核心采集流程（Java实现）**:
```java
@Service
public class ProductCollectorService {
    
    @Autowired
    private TakealotCollector takealotCollector;
    
    @Autowired
    private BrandFilterService brandFilter;
    
    @Autowired
    private ProductService productService;
    
    @Async("taskExecutor")
    public CompletableFuture<CollectionResult> collectProducts(CollectionTask task) {
        // 1. 获取分类URL
        // 2. 发送HTTP请求获取商品列表页
        // 3. 使用Jsoup解析HTML
        // 4. 提取商品信息
        // 5. 品牌过滤
        // 6. 保存到数据库
        // 7. 记录采集日志
    }
}
```

**HTTP客户端配置**:
```java
@Configuration
public class HttpClientConfig {
    
    @Bean
    public HttpClient httpClient() {
        return HttpClient.newBuilder()
            .connectTimeout(Duration.ofSeconds(10))
            .followRedirects(Redirect.NORMAL)
            .build();
    }
}
```

---

## 5. 数据模型

### 5.1 商品表 (products)
```sql
CREATE TABLE products (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    tsin VARCHAR(50) NOT NULL COMMENT 'Takealot商品唯一标识',
    product_url VARCHAR(500) COMMENT '商品页面URL',
    main_category VARCHAR(100) COMMENT '主分类',
    lowest_category VARCHAR(100) COMMENT '最低分类',
    category_id BIGINT COMMENT '关联分类ID',
    product_title VARCHAR(500) NOT NULL COMMENT '商品标题',
    subtitle VARCHAR(500) COMMENT '副标题',
    description TEXT COMMENT '商品描述',
    whats_in_box TEXT COMMENT '包装清单',
    brand VARCHAR(100) COMMENT '品牌',
    warranty_type VARCHAR(50) COMMENT '保修类型',
    warranty_period INT COMMENT '保修期限（月）',
    image_urls JSON COMMENT '商品图片URL数组（JSON格式，最多5张）',
    rating DECIMAL(2,1) COMMENT '评分（1-5星）',
    review_count INT COMMENT '评论数量',
    price DECIMAL(10,2) COMMENT '价格',
    currency VARCHAR(10) DEFAULT 'ZAR' COMMENT '货币单位',
    video_url VARCHAR(500) COMMENT '视频链接',
    collected_at TIMESTAMP NOT NULL COMMENT '数据采集时间',
    task_id BIGINT COMMENT '关联采集任务ID',
    is_filtered BOOLEAN DEFAULT FALSE COMMENT '是否被品牌过滤',
    filter_reason VARCHAR(200) COMMENT '过滤原因',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY uk_tsin (tsin),
    INDEX idx_category (main_category, lowest_category),
    INDEX idx_brand (brand),
    INDEX idx_collected_at (collected_at),
    INDEX idx_task_id (task_id),
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

### 5.2 分类表 (categories)
```sql
CREATE TABLE categories (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    name VARCHAR(100) NOT NULL COMMENT '分类名称',
    parent_id BIGINT COMMENT '父分类ID（顶级分类为NULL）',
    level INT COMMENT '分类层级（1-3）',
    url VARCHAR(500) COMMENT '分类URL',
    source VARCHAR(50) DEFAULT 'takealot' COMMENT '数据来源',
    status VARCHAR(20) DEFAULT 'active' COMMENT '状态：active-启用，inactive-禁用',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_parent (parent_id),
    INDEX idx_level (level),
    FOREIGN KEY (parent_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品分类表';
```

### 5.3 采集任务表 (collection_tasks)
```sql
CREATE TABLE collection_tasks (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    task_name VARCHAR(200) NOT NULL COMMENT '任务名称',
    task_type VARCHAR(20) NOT NULL COMMENT '任务类型：MANUAL-手动，SCHEDULED-定时',
    status VARCHAR(20) DEFAULT 'PENDING' COMMENT '状态：PENDING-待执行，RUNNING-执行中，COMPLETED-完成，FAILED-失败',
    category_filter JSON COMMENT '分类筛选条件（JSON数组）',
    brand_filter TEXT COMMENT '品牌过滤规则（JSON格式）',
    max_products INT DEFAULT 100 COMMENT '最大采集数量',
    actual_count INT DEFAULT 0 COMMENT '实际采集数量',
    success_count INT DEFAULT 0 COMMENT '成功数量',
    failed_count INT DEFAULT 0 COMMENT '失败数量',
    filtered_count INT DEFAULT 0 COMMENT '被过滤数量',
    started_at TIMESTAMP COMMENT '开始时间',
    completed_at TIMESTAMP COMMENT '完成时间',
    duration_seconds INT COMMENT '执行时长（秒）',
    error_message TEXT COMMENT '错误信息',
    cron_expression VARCHAR(100) COMMENT '定时表达式（仅定时任务）',
    next_run_time TIMESTAMP COMMENT '下次执行时间（仅定时任务）',
    is_enabled BOOLEAN DEFAULT TRUE COMMENT '是否启用（仅定时任务）',
    created_by VARCHAR(100) COMMENT '创建人',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_status (status),
    INDEX idx_type (task_type),
    INDEX idx_next_run (next_run_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='采集任务表';
```

### 5.4 采集日志表 (collection_logs)
```sql
CREATE TABLE collection_logs (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    task_id BIGINT NOT NULL COMMENT '关联任务ID',
    tsin VARCHAR(50) COMMENT '商品TSIN',
    product_title VARCHAR(500) COMMENT '商品标题',
    product_url VARCHAR(500) COMMENT '商品URL',
    status VARCHAR(20) NOT NULL COMMENT '状态：SUCCESS-成功，FAILED-失败，SKIPPED-跳过，FILTERED-被过滤',
    message TEXT COMMENT '日志信息/错误详情',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    response_time_ms INT COMMENT '响应时间（毫秒）',
    proxy_used VARCHAR(200) COMMENT '使用的代理',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_task_id (task_id),
    INDEX idx_status (status),
    FOREIGN KEY (task_id) REFERENCES collection_tasks(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='采集日志表';
```

### 5.5 品牌黑名单表 (brand_blacklist)
```sql
CREATE TABLE brand_blacklist (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    brand_name VARCHAR(100) NOT NULL COMMENT '品牌名称（模糊匹配关键词）',
    brand_type VARCHAR(50) COMMENT '品牌类型：ELECTRONICS-电子产品，FASHION-时尚，OTHER-其他',
    reason VARCHAR(200) COMMENT '加入黑名单原因',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否生效',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY uk_brand_name (brand_name),
    INDEX idx_type (brand_type),
    INDEX idx_active (is_active)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='品牌黑名单表';
```

---

## 6. API接口规范

### 6.1 商品接口

#### 6.1.1 获取商品列表
```
GET /api/products
```

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页数量，默认20 |
| category | string | 否 | 分类筛选 |
| brand | string | 否 | 品牌筛选 |
| keyword | string | 否 | 关键词搜索 |
| sort | string | 否 | 排序字段，默认collected_at |
| order | string | 否 | 排序方向：asc/desc，默认desc |

**响应示例**:
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "id": 1,
        "tsin": "12345678",
        "productTitle": "Smartphone XYZ",
        "mainCategory": "Electronics",
        "brand": "Unknown Brand",
        "price": 2999.00,
        "rating": 4.5,
        "collectedAt": "2024-02-16T10:30:00",
        "imageUrls": ["https://...", "https://..."]
      }
    ],
    "totalElements": 100,
    "totalPages": 5,
    "size": 20,
    "number": 1
  }
}
```

#### 6.1.2 获取商品详情
```
GET /api/products/{id}
```

### 6.2 采集任务接口

#### 6.2.1 创建采集任务
```
POST /api/collection-tasks
```

#### 6.2.2 手动执行采集任务
```
POST /api/collection-tasks/{id}/execute
```

#### 6.2.3 获取任务列表
```
GET /api/collection-tasks
```

---

## 7. 优先级规划

### 7.1 必须实现 (Must Have) - P0
- [ ] 商品数据采集并写入数据库（Java实现）
- [ ] 商品表和分类表设计实现
- [ ] 商品列表和详情API接口
- [ ] 前端商品展示页面
- [ ] 品牌黑名单过滤（Java实现）

### 7.2 应该实现 (Should Have) - P1
- [ ] 采集任务表和日志表
- [ ] 手动触发采集任务API
- [ ] 定时自动采集任务（Spring Scheduler）
- [ ] 采集任务管理页面
- [ ] HTTP代理配置

### 7.3 可以实现 (Could Have) - P2
- [ ] 采集进度实时监控（WebSocket）
- [ ] 数据导出Excel功能
- [ ] 多平台采集支持
- [ ] 数据可视化报表

### 7.4 未来规划 (Won't Have Now)
- [ ] 价格变动历史记录
- [ ] 库存监控
- [ ] AI智能分类

---

## 8. 验收标准

### 8.1 功能验收
- [ ] 商品数据成功采集并写入数据库（Java实现）
- [ ] API接口返回正确的商品数据
- [ ] 前端页面能正常展示商品列表和详情
- [ ] 定时任务按配置自动执行（Spring Scheduler）
- [ ] 品牌过滤准确率>95%（Java实现）

### 8.2 性能验收
- [ ] API响应时间<500ms
- [ ] 页面加载时间<2秒
- [ ] 单次采集100+商品稳定运行
- [ ] 数据库查询支持100万+数据

### 8.3 质量验收
- [ ] 数据字段完整性>90%
- [ ] 采集时间准确记录
- [ ] 无知名品牌商品漏过滤

---

**文档结束**
