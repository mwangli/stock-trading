server:
  port: 8080

spring:
  application:
    name: model-service-test

  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://124.220.36.95:6033/stock_trading?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Root.123456

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  # Redis配置
  data:
    redis:
      host: 124.220.36.95
      port: 6379
      password: Root.123456
      database: 0

  # MongoDB配置
    mongodb:
      host: 124.220.36.95
      port: 27017
      database: stock_trading
      authentication-database: admin
      username: admin
      password: Root.123456

# LSTM 模型训练配置
models:
  lstm:
    model-path: models/lstm-stock-test
    sequence-length: 60
    hidden-size: 50
    num-layers: 2
    epochs: 10
    batch-size: 32
    learning-rate: 0.001
    dropout: 0.2
    train-ratio: 0.8
    input-size: 5
    early-stopping: true
    patience: 5

# 日志配置
logging:
  level:
    root: INFO
    com.stock: DEBUG