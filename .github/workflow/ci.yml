name: deploy

on: [ push ]

jobs:
  git-mirror:
    runs-on: ubuntu-latest
    steps:
      - name: test
#        env:
#          SSH_PRIVATE_KEY: ${{ secrets.REGISTRY }}
        run: |
          echo "github action test"