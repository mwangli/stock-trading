# 04-交易执行需求文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 模块 | 交易执行模块 (Executor) |
| 包路径 | com.stock.tradingExecutor |
| 版本 | **3.0.0** |
| 最后更新 | **2026-03-08** |

**重要变更 (v3.0)**:
- ✅ **新增动态任务调度需求**: 任务配置持久化、Cron 可变更、启用/禁用、手动触发、执行状态记录
- ✅ **新增 WebSocket 实时推送需求**: 通知通道与日志通道、前端实时接收
- ✅ **新增订单通知需求**: 订单成交/拒绝/取消后通过事件驱动推送前端

**重要变更 (v2.0)**:
- ✅ **新增价格监控需求**: 买入/卖出价格监控策略、价格采样机制、尾盘强制执行
- ✅ **新增订单跟踪需求**: 订单状态轮询、超时撤单、失败重试
- ✅ **新增交易成本需求**: 手续费计算、成本记录
- ✅ **新增证券平台对接需求**: 券商API适配、账户同步、委托管理
- ✅ **新增执行控制需求**: 并行交易、任务中断

## 1. 模块概述

执行模块是系统的交易执行层，负责风控检查、价格监控、订单执行和持仓管理。

## 2. 功能需求

### 2.1 风控检查

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-001 | 止损检查 (日亏损/月亏损/个股止损) | P0 | 阈值可配置 |
| EXECUTOR-REQ-002 | 仓位检查 (单股/总仓位/行业集中度) | P0 | 不超限 |
| EXECUTOR-REQ-003 | 熔断检查 (市场异常时停止交易) | P0 | 及时响应 |
| EXECUTOR-REQ-004 | T+1 规则检查 | P0 | 当日买入次日才能卖 |
| EXECUTOR-REQ-005 | 涨跌停限制检查 | P0 | 涨停不买，跌停不卖 |

### 2.2 订单执行

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-006 | 生成买入/卖出订单 | P0 | 订单信息完整 |
| EXECUTOR-REQ-007 | 执行交易 (模拟/实盘) | P0 | 支持两种模式 |
| EXECUTOR-REQ-008 | 订单状态跟踪 | P0 | 状态包含已报/已成/部分成交/已撤/废单 |
| EXECUTOR-REQ-009 | 撤单处理 | P0 | 支持超时自动撤单和手动撤单 |

### 2.3 持仓管理

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-010 | 持仓增减更新 | P0 | 准确更新 |
| EXECUTOR-REQ-011 | 持仓成本计算 | P0 | 加权平均成本 |
| EXECUTOR-REQ-012 | 浮动盈亏统计 | P0 | 实时计算 |
| EXECUTOR-REQ-013 | 持仓分布分析 | P1 | 按行业/市值等 |

### 2.4 账户管理

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-014 | 可用余额管理 | P0 | 准确计算 |
| EXECUTOR-REQ-015 | 资金冻结/解冻 | P0 | 下单冻结，成交扣减 |
| EXECUTOR-REQ-016 | 资金流水记录 | P0 | 记录完整 |

### 2.5 交易记录

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-017 | 交易历史记录 | P0 | 记录所有成交 |
| EXECUTOR-REQ-018 | 委托记录管理 | P0 | 记录所有委托 |
| EXECUTOR-REQ-019 | 交易统计查询 | P1 | 支持多维度统计 |

### 2.6 价格监控需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-020 | 买入价格监控 | P0 | 价格低于均价(1-N%)时触发买入，N可配置 |
| EXECUTOR-REQ-021 | 卖出价格监控 | P0 | 价格高于均价(1+N%)时触发卖出，N可配置 |
| EXECUTOR-REQ-022 | 价格采样机制 | P0 | 定时采样(10-20秒)，计算滚动均价 |
| EXECUTOR-REQ-023 | 尾盘强制执行 | P0 | 14:55后强制执行未完成订单 |

### 2.7 订单跟踪需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-024 | 订单状态轮询 | P0 | 每10秒轮询，最多18次 |
| EXECUTOR-REQ-025 | 超时自动撤单 | P0 | 60秒未成交自动撤单 |
| EXECUTOR-REQ-026 | 失败重试机制 | P0 | 最多重试10次，可配置 |

### 2.8 交易成本需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-027 | 手续费计算 | P0 | 万分之五，最低5元，费率可配置 |
| EXECUTOR-REQ-028 | 成本记录 | P0 | 手续费记录到订单和交易记录 |

### 2.9 证券平台对接需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-029 | 账户资金同步 | P0 | 实时获取可用资金、冻结资金、总资产 |
| EXECUTOR-REQ-030 | 实时行情获取 | P0 | 获取股票当前价格 |
| EXECUTOR-REQ-031 | 委托下单接口 | P0 | 支持买入/卖出委托提交 |
| EXECUTOR-REQ-032 | 委托查询接口 | P0 | 查询委托状态(已报/已成/已撤/废单) |
| EXECUTOR-REQ-033 | 委托撤单接口 | P0 | 撤销未成交委托 |
| EXECUTOR-REQ-034 | 多券商适配 | P1 | 支持切换不同券商实现 |

### 2.10 执行控制需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-035 | 并行交易执行 | P0 | 支持多股票同时交易，线程池管理 |
| EXECUTOR-REQ-036 | 任务中断机制 | P1 | 支持手动中断正在执行的任务 |

### 2.11 动态任务调度需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-037 | 任务配置持久化 | P0 | 任务列表存库，支持启用/禁用、Cron 表达式 |
| EXECUTOR-REQ-038 | 动态 Cron 变更 | P0 | 修改 Cron 后无需重启即可生效 |
| EXECUTOR-REQ-039 | 任务启用/禁用 | P0 | 可暂停、恢复指定任务 |
| EXECUTOR-REQ-040 | 手动触发执行 | P0 | 支持对指定任务立即执行一次 |
| EXECUTOR-REQ-041 | 执行状态记录 | P0 | 记录上次执行时间、耗时、成功/失败及错误信息 |
| EXECUTOR-REQ-042 | 启动时加载任务 | P0 | 应用启动后自动加载启用任务并注册到调度器 |

### 2.12 WebSocket 实时推送需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-043 | 通知通道 | P0 | 提供 `/ws/notifications`，供前端接收业务通知（如订单结果） |
| EXECUTOR-REQ-044 | 日志通道 | P0 | 提供 `/ws/logs`，供前端接收实时日志流 |
| EXECUTOR-REQ-045 | 广播能力 | P0 | 服务端可向所有已连接客户端广播文本消息 |
| EXECUTOR-REQ-046 | 连接管理 | P0 | 连接建立/关闭时维护会话集合，单路发送失败不影响其他连接 |

### 2.13 订单通知需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-047 | 订单事件发布 | P0 | 买入/卖出执行后发布订单结果事件（类型、结果对象） |
| EXECUTOR-REQ-048 | 事件转推送 | P0 | 监听订单事件，将结果格式化为通知消息经 WebSocket 推送 |
| EXECUTOR-REQ-049 | 通知内容 | P0 | 包含类型(BUY/SELL)、级别(success/error/info)、标题、内容、时间戳及订单数据 |
| EXECUTOR-REQ-050 | 异步处理 | P0 | 通知发送异步执行，不阻塞交易主流程 |

### 2.14 滑块验证码破解需求

| 需求 ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| EXECUTOR-REQ-051 | 验证码图片获取 | P0 | 能够从券商平台获取滑块验证码的背景图和滑块图 |
| EXECUTOR-REQ-052 | 缺口位置计算 | P0 | 通过图像识别算法计算滑块需要移动的距离 |
| EXECUTOR-REQ-053 | 滑动轨迹生成 | P0 | 生成模拟人工滑动的轨迹曲线，避免被检测为机器操作 |
| EXECUTOR-REQ-054 | 验证码提交验证 | P0 | 将滑动结果提交给券商平台进行验证 |
| EXECUTOR-REQ-055 | 登录流程集成 | P0 | 在券商平台登录流程中自动处理滑块验证码 |
| EXECUTOR-REQ-056 | 识别失败重试 | P1 | 验证码识别失败时支持自动重试机制 |

## 3. 风控规则

### 3.1 止损规则

| 规则名 | 阈值 | 说明 |
|--------|------|------|
| 日亏损限制 | 3% | 当日累计亏损超过 3% 禁止买入 |
| 月亏损限制 | 10% | 当月累计亏损超过 10% 禁止买入 |
| 个股止损 | 8% | 单只股票亏损超过 8% 触发卖出 |
| 总止损 | 15% | 总账户亏损超过 15% 清仓 |

### 3.2 仓位规则

| 规则名 | 阈值 | 说明 |
|--------|------|------|
| 单股仓位上限 | 30% | 单只股票市值不超过总资产 30% |
| 总仓位上限 | 80% | 股票总市值不超过总资产 80% |
| 最低仓位 | 20% | 保持至少 20% 现金 |
| 行业集中度 | 50% | 同一行业股票不超过 50% |

### 3.3 交易限制

| 规则名 | 限制 | 说明 |
|--------|------|------|
| 单笔最小金额 | 1000 元 | 低于此金额不执行 |
| 单日交易次数 | 10 次 | 超过次数禁止交易 |
| T+1 规则 | 强制 | 当日买入次日才能卖出 |
| 涨跌停限制 | 强制 | 涨停不买入，跌停不卖出 |

## 4. 接口需求

### 4.1 风控检查接口

```java
// 风控检查接口
RiskCheckResult checkRisk(TradeRequest request);
```

### 4.2 交易执行接口

```java
// 交易执行接口
TradeResult executeBuy(TradeRequest request);
TradeResult executeSell(TradeRequest request);
```

### 4.3 持仓查询接口

```java
// 持仓查询接口
Position getPosition(String stockCode);
List<Position> getAllPositions();
```

### 4.4 账户查询接口

```java
// 账户查询接口
Account getAccount();
```

### 4.5 订单查询接口

```java
// 订单查询接口
Order getOrder(String orderId);
List<Order> getOrdersByStatus(OrderStatus status);
```

### 4.6 券商适配接口

```java
// 券商适配接口
public interface BrokerAdapter {
    // 获取账户资金信息
    AccountInfo getAccountInfo();
    // 获取股票实时价格
    Double getRealtimePrice(String stockCode);
    // 提交委托订单
    OrderResult submitOrder(String direction, String stockCode, Double price, Integer quantity);
    // 查询委托状态
    OrderStatus queryOrderStatus(String orderId);
    // 撤销委托
    Boolean cancelOrder(String orderId);
}
```

### 4.7 价格监控接口

```java
// 价格监控接口
public interface PriceMonitor {
    // 开始价格监控
    void startMonitor(String stockCode, String direction, MonitorConfig config);
    // 计算均价
    Double calculateAvgPrice();
    // 判断是否执行
    Boolean shouldExecute();
    // 是否临近收盘
    Boolean isNearClose();
    // 停止监控
    void stopMonitor();
}
```

### 4.8 动态任务调度接口

```java
// 任务管理 REST API
GET  /api/jobs               // 列表所有任务配置
POST /api/jobs/{id}/run      // 手动触发执行
POST /api/jobs/{id}/status   // 启用/禁用 (body: { "active": true|false })
POST /api/jobs/{id}/cron     // 更新 Cron (body: { "cron": "0 0 18 * * MON-FRI" })
```

### 4.9 WebSocket 与订单通知接口

```java
// WebSocket 端点
/ws/notifications   // 业务通知（订单成交、拒绝、取消等）
/ws/logs            // 实时日志推送

// 订单通知：TradeExecutor 发布 OrderNotificationEvent，NotificationListener 消费后
// 调用 NotificationService.notifyOrder(result, type)，经 NotificationWebSocketHandler 广播
```

## 5. 性能需求

| 指标 | 要求 |
|------|------|
| 风控检查响应 | < 50ms |
| 订单执行响应 | < 200ms |
| 持仓更新延迟 | < 1 秒 |
| 价格采样间隔 | 10-20 秒可配置 |
| 订单轮询间隔 | 10 秒 |
| 并发支持 | 支持 10 个并发请求 |

## 6. 依赖关系

### 6.1 内部依赖

- 策略模块：提供交易信号
- 数据采集模块：获取实时价格

## 7. 验收标准

### 7.1 功能验收

- [ ] 风控检查功能正常
- [ ] 订单执行功能正常
- [ ] 持仓管理准确
- [ ] 账户管理准确
- [ ] 交易记录完整
- [ ] 价格监控功能正常
- [ ] 订单轮询机制正常
- [ ] 手续费计算准确
- [ ] 券商接口对接正常
- [ ] 并行交易执行正常
- [ ] 动态任务调度：列表/启用禁用/Cron 修改/手动触发正常，执行状态正确回写
- [ ] WebSocket：/ws/notifications、/ws/logs 可连接并收到广播
- [ ] 订单通知：下单成交/拒绝/取消后前端能收到对应通知
- [ ] 滑块验证码破解：能够自动识别并通过券商平台滑块验证码

### 7.2 安全验收

- [ ] 风控规则严格执行
- [ ] 模拟/实盘隔离
- [ ] 异常交易拦截